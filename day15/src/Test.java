import java.util.Random;

public class Test {
    public static void main(String[] args) {

        //1.初始化大小为16的int类型数组(30分)
        for(int a=1;a<=16;a++){
            for(int b=1;b<a-1;b++){
                System.out.println(a%b);
            }
            //2. 使用for循环给数组赋值（随机数）(30分)
            String its=new String();
            its.substring(its.lastIndexOf(Random));

        }
        //3. 使用冒泡排序对赋值后的数组进行降序排序（40分）
        System.out.println(sum.desc);
    }
}
